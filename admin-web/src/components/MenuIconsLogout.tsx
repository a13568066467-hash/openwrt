import { Logout } from '@icon-park/react';

export function LogoutIcon() {
  return (
    <span className="menu-icon menu-icon--sm">
      <Logout theme="multi-color" size={18} fill={['#64748b', '#94a3b8', '#fff', '#cbd5e1']} strokeWidth={3} />
    </span>
  );
}
